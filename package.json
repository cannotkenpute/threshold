{
  "name": "threshold",
  "version": "0.1.0",
  "private": true,
  "description": "THRESHOLD — vanilla JS ES-module Three.js horror game (no framework, no build step)",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "lint": "node scripts/lint/check-syntax.mjs",
    "lint:manifests": "node scripts/lint/validate-manifests.mjs",
    "build:monsters": "node scripts/monsters/build-monster-assets.mjs",
    "validate:monsters": "node scripts/monsters/validate-monster-manifest.mjs",
    "test:logic": "node scripts/test/multiplayer-logic.mjs && node scripts/test/lobby-logic.mjs && node scripts/test/monster-logic.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@gltf-transform/cli": "4.4.2"
  }
}
